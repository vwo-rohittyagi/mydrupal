services:
  vwo.service:
    class: Drupal\vwo\Service\VwoService
    arguments: ['@config.factory', '@current_user', '@path.matcher', '@path.current', '@entity_type.manager', '@current_route_match']
